import csv
import sys
import random

teams = []
teams = [0 for i in range(16)]
teams[0] = {'name' : 'Gil', 'age' : '20', 'Sex' : 'Male'}
print(teams[0])
#testing if my commit it's going to the repository online